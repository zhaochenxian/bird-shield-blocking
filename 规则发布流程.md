## 鸟盾规则修改与发布流程（固定复用）

适用目标：
- 所有规则修改先在 `rules-improve` 分支完成
- 验证通过后再合并到 `main`

补充记录：
- 详细教程已放到项目目录下面，后续按本流程文件与目录教程配合执行。

---

### 0) 开始前同步

```bash
git checkout rules-improve
git pull origin rules-improve
```

如果本地不在 `rules-improve`，先切过去再同步。

---

### 1) 修改规则

只改现有分类文件（如 `share/keywords/*.txt`），不要新增插件不识别的新分类路径。

建议：
- 同类规则尽量合并，减少重复
- 规则格式使用：`处理动作+权重\\条件表达式`，例如 `mark320\\...`
- 优先加组合条件（关键词 + 账号特征）降低误伤

#### 1.1 已验证可复用规则（直接套用）

1) AI机器人类（文件：`bird-shield-blocking/share/keywords/AI机器人.txt`）

```txt
mark260\\(foc<120&frc<120&cd<2y)&txt=/^\s*找个(长期)?搭子[。\.·\s]*$|^\s*找个(温柔)?主[。\.·\s]*$|找个(长期)?搭子|找个(温柔)?主/
```

2) 诈骗引流类（文件：`bird-shield-blocking/share/keywords/fraud.txt`）

```txt
mark320\\(foc<800&frc<800&cd<5y)&txt=/([QqＱｑ]|扣[\s\+\-]*)[裙群][\s\.:：]*\d{6,12}/&txt=/文.{0,2}爱|裸.{0,2}聊|调.{0,2}教|男.{0,2}交|足.{0,2}交|约.{0,2}炮|女.{0,2}友|私.{0,2}聊|裸|做爱|爆料|福利|交友|[sSｍＭmM].{0,2}控|[XxＸｘ][\s:：]*@\w{4,}|@\w{4,}/
```

3) 疑似色情类（文件：`bird-shield-blocking/share/keywords/suspectedPornography.txt`）

- 当前不再放“搭子/找主”和“Q群引流”规则，避免与上面两类重复命中。
- 该文件保留传统色情词和站点特征规则（按现有仓库内容维护）。

4) 分类判断速记

- `找搭子/找主/主奴口吻` → AI机器人
- `Q群号/扣群号 + 联系方式/擦边词` → 诈骗
- `纯色情关键词、站点、性行为词` → 疑似色情

#### 1.2 具体规则写法（按手册）

基本格式：

```txt
处理动作+权重\\条件表达式
```

可直接套用模板：

```txt
mark320\\(foc<800&frc<800&cd<5y)&txt=/([QqＱｑ]|扣[\s\+\-]*)[裙群][\s\.:：]*\d{6,12}/&txt=/文.{0,2}爱|裸.{0,2}聊|调.{0,2}教|男.{0,2}交|足.{0,2}交|约.{0,2}炮|女.{0,2}友|私.{0,2}聊|裸|做爱|爆料|福利|交友|[sSｍＭmM].{0,2}控|[XxＸｘ][\s:：]*@\w{4,}|@\w{4,}/
mark260\\(foc<120&frc<120&cd<2y)&txt=/^\s*找个(长期)?搭子[。\.·\s]*$|^\s*找个(温柔)?主[。\.·\s]*$|找个(长期)?搭子|找个(温柔)?主/
```

处理动作建议：

- `mark`：先标记观察（推荐默认）
- `filter`：进入待确认
- `block`：高置信度时直接拉黑
- `skip`：命中后跳过后续规则（仅用于白名单/排除逻辑）

---

### 2) 本地检查

```bash
git status --short
```

确认改动范围只包含预期文件。

---

### 3) 提交到开发分支

```bash
git add <修改的文件>
git commit -m "<本次规则说明>"
git push origin rules-improve
```

示例：

```bash
git add bird-shield-blocking/share/keywords/fraud.txt
git commit -m "增强Q群引流规则抗拆字"
git push origin rules-improve
```

---

### 4) 合并发布到 main

```bash
git checkout main
git pull origin main
git merge --no-ff rules-improve -m "Merge branch 'rules-improve' into main"
git push origin main
```

发布后回到开发分支：

```bash
git checkout rules-improve
```

---

### 5) 非快进冲突（push 被拒）处理

当 `git push` 报 `non-fast-forward`：

```bash
git pull --rebase origin <当前分支>
git push origin <当前分支>
```

分支名替换为 `rules-improve` 或 `main`。

---

### 6) 每次完成后的结果记录（建议）

记录三项即可：
- 分支：`rules-improve` / `main`
- 提交号（commit hash）
- 改动文件列表

---

### 一句话流程

`rules-improve` 修改与提交 → 推送 `rules-improve` → 合并到 `main` → 推送 `main` → 切回 `rules-improve`。
