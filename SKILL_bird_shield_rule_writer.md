# 鸟盾规则仓库 · AI 写入规则 Skill

> 本文档是 AI 向鸟盾规则仓库（bird-shield-blocking）添加/维护规则时必须遵守的完整工作流程与标准。
> 每次修改规则文件前，必须先通读本文档，按照"分级体系 → 账号画像 → 关键词强度"三个维度组合生成规则。

---

## 一、鸟盾表达式速查

### 1.1 规则行格式

```
动作权重\\账号条件&内容条件
```

- **动作**：`block`（拉黑）、`filter`（待确认）、`mark`（标记）、`skip`（跳过后续规则）
- **权重**（数字）：越大越先执行，范围建议 100–2000
- `\\` 后为完整条件表达式，逻辑运算符 `&`（与）`|`（或）`!`（非），括号调整优先级

### 1.2 常用账号维度字段

| 字段 | 缩写 | 含义 |
|------|------|------|
| `followers_count` | `foc` | 粉丝数 |
| `friends_count` | `frc` | 关注数 |
| `created_date` | `cd` | 注册日期（`<6m` = 6个月内） |
| `blue` | — | 蓝V认证（`!blue` = 非蓝V） |
| `following` | `fi` | 已关注我 |
| `followed_by` | `fb` | 我关注他 |

### 1.3 常用内容维度字段

| 字段 | 缩写 | 含义 |
|------|------|------|
| `name` | `@` | 用户名称，`@*=xxx` 表示包含 |
| `description` | `desc` | 简介，`desc*=xxx` |
| `tweet_text` | `txt` | 推文正文 |
| `urls` | `url` | 推文链接 |
| `any` | — | 名称+简介+推文全文匹配 |

文本运算符：`=`（等于）、`*=`（包含）、`^=`（开头）、`$=`（结尾）、`!=`（不等于）、`/正则/`（正则）

---

## 二、分级体系定义

所有规则文件均按以下五级强度组织，**每个规则行必须标注其等级注释**。

### 等级说明

| 等级 | 权重范围 | 动作 | 适用场景 |
|------|----------|------|----------|
| **L1 – 确定拦截** | 1000–2000 | `block` | 极高置信度：信号极强，几乎无误判，直接拉黑 |
| **L2 – 高可信过滤** | 700–999 | `block` | 高置信度：多维度交叉验证，强烈特征组合 |
| **L3 – 中等过滤** | 400–699 | `filter` | 中等置信度：单维度强特征或多维度弱组合，需标记确认 |
| **L4 – 弱信号标记** | 100–399 | `mark` | 低置信度：疑似特征，仅标记提示，不主动处理 |
| **L5 – 白名单跳过** | 900–2000 | `skip` | 可信用户（蓝V、已互关等），跳过后续规则 |

> **原则**：L5 白名单规则必须排在同文件最高权重（≥ L1 权重），先放行可信用户，再依次检查 L1–L4。

### 账号画像标准模板

AI 写规则时，账号可信度评估**必须**使用以下三档组合，不得自行发明新组合：

```
# 高风险账号（三弱）：粉丝少 & 新号 & 非认证非老关系
(foc<10 & cd<6m & !blue & frc<30)

# 中风险账号（二弱）：满足任意两个弱指标
(foc<50 & cd<1y) | (foc<20 & !blue) | (cd<3m & !blue)

# 低风险账号（一弱）：仅粉丝少 或 仅新号
foc<100 | cd<6m
```

---

## 三、规则文件写入规范

### 3.1 文件头注释

每个 `.txt` 规则文件顶部必须有如下注释块：

```
# ======================================================
# 规则文件：[文件名].txt
# 分类：[分类描述]
# 更新日期：YYYY-MM-DD
# 说明：[本规则文件的拦截目标与覆盖范围]
# ======================================================
```

### 3.2 每级规则块格式

```
# --- L1 确定拦截（权重 1000-2000）---
权重\\账号条件&内容条件
...

# --- L2 高可信过滤（权重 700-999）---
...

# --- L3 中等过滤（权重 400-699）---
...

# --- L4 弱信号标记（权重 100-399）---
...

# --- L5 白名单跳过（优先执行）---
...
```

### 3.3 关键词强度分级

写关键词/正则时，按照以下强度分配等级：

| 关键词强度 | 等级 | 判断依据 |
|------------|------|----------|
| 专有垃圾词汇（极罕见于正常语境） | L1 | 如"跑分平台"、"调教母狗"、"购买粉丝" |
| 行业黑话+账号弱指标 | L2 | 如"绿泡泡约"+"新号" |
| 模糊敏感词+多条件组合 | L3 | 如"私信我"+"粉丝<50" |
| 单一模糊词 | L4 | 如只含"加我"或"私聊" |

### 3.4 正则写法规范

- 中文词汇之间加 `\s*` 以兼容空格/换行绕过
- 复杂交替用 `(a|b|c)` 而非多行重复
- 在账号条件上明确括号，避免运算符优先级歧义

---

## 四、AI 添加规则的标准流程（SOP）

```
1. 接收需求
   └─ 确认：分类文件名、目标骚扰/欺诈/机器人类型、样本推文

2. 评估账号画像
   └─ 选择：高风险(三弱) / 中风险(二弱) / 低风险(一弱) 模板

3. 提取关键词特征
   └─ 分配关键词等级（L1–L4）

4. 起草规则行
   └─ 格式：动作权重\\账号条件&(txt=/正则/|@*=文本|desc*=文本)
   └─ 确认权重在本等级范围内

5. 插入文件
   └─ 在对应 # --- L? --- 注释块下方插入
   └─ 同一等级内权重从高到低排序

6. 自查
   └─ 确认 L5 白名单是否覆盖误伤场景
   └─ 确认正则语法正确（无多余括号、转义正确）
   └─ 确认不与已有规则重复

7. 输出变更说明
   └─ 列出：新增/修改的行、等级、权重、理由
```

---

## 五、各分类文件说明

| 文件 | 分类目标 | 主要特征维度 |
|------|----------|-------------|
| `AI机器人.txt` | 色情机器人/钓鱼机器人 | 用户名+简介中的诱导词、新号高频特征 |
| `fraud.txt` | 金融诈骗/跑分/博彩 | 简介/推文中的平台词、博彩专有名词 |
| `harassment.txt` | 骚扰行为 | 网盘链接、私信诱导、高频骚扰模式 |
| `moderatePornography.txt` | 中度色情 | 色情关键词组合 |
| `extremePornography.txt` | 极端色情 | 极端色情专有词 |
| `politicalPropaganda.txt` | 政治宣传 | 政治口号/政治人物词 |
| `profanity.txt` | 粗口辱骂 | 骂人词汇 |

---

## 六、示例：完整规则块（以 fraud.txt 为例）

```txt
# ======================================================
# 规则文件：fraud.txt
# 分类：金融诈骗 / 跑分 / 博彩 / 诱导付款
# 更新日期：2026-03-24
# 说明：覆盖博彩平台推广、跑分招募、虚假平台诱导、
#       网络诈骗相关账号与推文
# ======================================================

# --- L5 白名单跳过（优先执行）---
skip 2000 \\ blue & fi
skip 1800 \\ blue & fb

# --- L1 确定拦截（权重 1000-2000）---
block 1500 \\ (foc<10&cd<6m&!blue&frc<30)&info=/跑分|洗钱|空包/
block 1200 \\ (foc<10&cd<6m&!blue&frc<30)&info=/已入.{0,3}平台|只在平台上约|绿泡泡/

# --- L2 高可信过滤（权重 700-999）---
block 900 \\ (foc<50&cd<1y)&(@*=六合彩|@*=博彩)
block 800 \\ (foc<20&!blue)&txt=/跑分|平台返佣|招募代理/

# --- L3 中等过滤（权重 400-699）---
filter 600 \\ (foc<100|cd<6m)&txt=/私信有惊喜|添加.*获利|高额返佣/
filter 500 \\ (foc<50&!blue)&url=/xn88\.(vip|com|org)/

# --- L4 弱信号标记（权重 100-399）---
mark 300 \\ foc<100&txt=/加我|私聊|有意者/&txt=/赚钱|获利|佣金/
```

---

> 文档版本：v1.0 · 2026-03-24
> 维护者：bird-shield-blocking 项目
